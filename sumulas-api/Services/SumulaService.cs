using Microsoft.Extensions.Configuration;
using MongoDB.Driver;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace sumulas_api.Services
{
    public class SumulaService
    {
        //private readonly IMongoCollection<Campeonato> _books;

        //public SumulaService(IConfiguration config)
        //{
        //    var client = new MongoClient(config.GetConnectionString("sumulas"));
        //    var database = client.GetDatabase("sumulas");
        //    _books = database.GetCollection<Campeonato>("campeonatos");
        //}
    }
}
